# CASCADE Regeln - AI Assistant Memory

## 📚 **WICHTIG: DWEapp.md ist die zentrale Wahrheitsquelle!**

**🔔 ZUERST in `DWEapp.md` nachschauen:**
- **Architektur & Ordnerstruktur** → Wo ist welche Datei?
- **Komponenten Übersicht** → Was gibt es bereits?
- **API Integration** → Wie funktioniert WeClapp?
- **Version History** → Was wurde wann geändert?

**📋 Alle Dokumentationen im Überblick:**
- **`DWEapp.md`** → Hauptdokumentation (Architektur, Komponenten, API)
- **`WORKFLOW.md`** → Deployment & Entwicklung (CI/CD, Docker)
- **`DATABASE.md`** → Datenbankschema & Prisma
- **`.cascade-rules.md`** → Diese Datei (AI Assistant Regeln)

**📋 DOKUMENTATIONS-REGELN:**
- **Immer `DWEapp.md` aktualisieren** bei Änderungen
- **Neue Komponenten** dort dokumentieren
- **API Änderungen** eintragen
- **Version History** pflegen

---

## 🚨 **IMMER VOR CODE SCHREIBEN PRÜFEN:**

### **1. Datei-Größen (nach Typ)**

| Dateityp | Ideal | Maximum | Warum |
|----------|-------|---------|-------|
| **React Component** | 80–150 | 250 | Eine UI-Aufgabe |
| **Custom Hook** | 40–120 | 200 | Eine Logik-Aufgabe |
| **API-Funktion** | 20–80 | 150 | Ein Endpoint |
| **Utility-Funktion** | 10–50 | 100 | Helper bleiben klein |
| **Type/Interface** | 5–50 | 100 | Nur Typen, keine Logik |
| **Test-Datei** | 50–200 | 400 | Tests können länger sein |

**Über Maximum → aufteilen!**

### **2. CSS-Regeln**
- ❌ `bg-white`, `text-gray-900`, `border-red-500`
- ✅ `bg-[var(--bg-primary)]`, `text-[var(--primary)]`, `border-[var(--error)]`

### **3. UI-Komponenten**
- ✅ `import { Button } from '@/components/ui/button'`
- ✅ `import { Plus, User, Package } from 'lucide-react'`
- ✅ `import { useReactTable } from '@tanstack/react-table'`
- ✅ **WIEDERVERWENDUNG**: Immer importieren, nicht neu bauen
- 🎨 **Design-System Demo**: `/design-system`
- 📋 **DEMO-REGEL**: Alle neuen UI-Komponenten zur Demo hinzufügen!
- ❌ Direkte Library-Imports

### **4. Deutsche Texte**
- UI-Texte: Deutsch
- Code: Englisch

### **5. Minimal Changes**
- Nur was nötig ändern
- Kein Over-Engineering

### **6. Dokumentations-Pflicht**
- **Immer `DWEapp.md` aktualisieren** bei Änderungen
- **Neue Komponenten** → Dateiliste eintragen
- **API Änderungen** → API-Sektion eintragen
- **Version Updates** → Version History eintragen

### **7. Design-System Demo Pflicht**
- **Immer `/design-system` Demo anpassen** bei UI-Komponenten Änderungen
- **Neue Komponenten** → Zur Demo hinzufügen
- **Geänderte Komponenten** → Demo aktualisieren
- **Alle Varianten** zeigen (mit/ohne Text, verschiedene Props)

---

## 🔄 **Mein Workflow:**

1. **📋 Regeln pruefen** (`.cascade-rules.md`)
2. **📚 Doku lesen** (`DWEapp.md` - Architektur, Komponenten, API)
3. **🤔 Ueberlegen** (Planung, CASCADE-Check)
4. **⚡ Ausfuehren** (Code schreiben, Tools nutzen)
5. **🔍 TypeScript-Check** (`npx tsc --noEmit` - ALLE Fehler auf einmal finden)
6. **🎨 Demo anpassen** (`/design-system` - UI-Komponenten zeigen)
7. **📝 Doku aktualisieren** (`DWEapp.md` - Aenderungen dokumentieren)

**🎯 Zuerst pruefen, dann denken, dann handeln, dann demonstrieren!**

---

## 🔍 **Debugging-Strategie (VOR Build!):**

**WICHTIG: Nie blind builden! Erst scannen, dann fixen!**

### Schritt 1: Alle Fehler scannen
```bash
npx tsc --noEmit
```

### Schritt 2: Fehler sammeln und gruppieren
- Alle Fehler notieren
- Nach Datei/Typ gruppieren
- Abhaengigkeiten erkennen

### Schritt 3: Alle Fehler auf einmal beheben
- Zusammenhaengende Fehler zusammen fixen
- Nicht nach jedem Fix neu builden
- Erst wenn ALLE behoben: Build starten

### Vorteile:
- Spart Zeit (kein wiederholtes Builden)
- Erkennt Zusammenhaenge zwischen Fehlern
- Verhindert Ping-Pong Fixes

---

## 🚀 **Deployment & Entwicklung:**

**Siehe `WORKFLOW.md` fuer vollstaendige Anleitung!**

### Deploy (CI/CD):
```bash
git push  # GitHub Actions deployed automatisch!
```

### Entwicklung (Windsurf SSH):
```bash
# 1. Windsurf: Ctrl+Shift+P → "Remote-SSH: Connect to Host"
# 2. Host: root@91.98.135.191
# 3. Ordner: /opt/weclapp-manager
# 4. Dev-Container starten:
docker compose --profile dev up -d dweapp-dev
```

### Container:
| Service | Port | Beschreibung |
|---------|------|--------------|
| dweapp | 3000 | Production |
| dweapp-dev | 3001 | Development |

---

## 🗄️ **Datenbank-Regeln (KRITISCH!):**

**Siehe `DATABASE.md` fuer vollstaendige Anleitung!**

### PostgreSQL Spalten-Namen:
- ❌ `lastLoginAt` (wird zu `lastloginat`!)
- ✅ `"lastLoginAt"` (bleibt camelCase!)

### Schema-Aenderungen:
```bash
# 1. Backup
docker exec weclapp-manager-db-1 pg_dump -U postgres dweapp > /tmp/backup.sql

# 2. Prisma sync
docker compose exec -T dweapp npx prisma db push --accept-data-loss

# 3. Neu deployen
git push
```

### PowerShell Problem:
- ❌ Komplexe SQL ueber SSH
- ✅ SQL in Datei auf Server, dann ausfuehren
- ✅ Besser: `prisma db push` verwenden!

---

## 📋 **Checklist vor jedem Tool-Call:**

```
□ DWEapp.md geprüft? (Architektur, Komponenten, API)
□ Dateigröße passend? (Component: 250, Hook: 200, API: 150, Utility: 100)
□ CSS-Variablen genutzt?
□ Zentrale UI-Komponenten?
□ **WIEDERVERWENDUNG**: Bestehende Komponenten genutzt?
□ **DEMO-REGEL**: Komponente zur Design-System Demo hinzugefügt?
□ **DEMO-ANPASSUNG**: /design-system aktualisiert?
□ Lucide Icons verwendet?
□ Deutsche UI-Texte?
□ Minimale Änderung?
□ DWEapp.md aktualisiert? (Änderungen dokumentiert)
```

---

*Bei Verstößen: STOPP → Korrigieren → Weitermachen*
